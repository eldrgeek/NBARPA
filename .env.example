# Netlify DB / Neon Database Configuration
# Get your database URL from Netlify dashboard or Neon console
# Format: postgresql://username:password@host/database?sslmode=require
VITE_DATABASE_URL=postgresql://username:password@host/database?sslmode=require

# Alternative: Use Neon-specific URL if different
VITE_NEON_DATABASE_URL=

# Supabase Configuration (if still using for auth)
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
